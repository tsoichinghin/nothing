if (get_application_name() == "PacketShare") or (get_window_name() == "PacketShare") then
    maximize();
end
if (get_application_name() == "PacketShare") or (get_window_name() == "PacketShare") then
    minimize();
end
