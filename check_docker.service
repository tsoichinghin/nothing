[Unit]
Description=Check if qmcgaw/gluetun container is running and take action

[Service]
Type=oneshot
ExecStart=/bin/bash -c 'if ! docker ps | grep -q "qmcgaw/gluetun"; then if [ "$(docker ps -q)" ]; then systemctl reboot; fi; fi'
